{
  "name": "hypersonic-api-server",
  "description": "Realtime backend server for processing microservice requests",
  "private": true,
  "scripts": {
    "start": "meteor run"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.36",
    "meteor-node-stubs": "^0.3.2"
  }
}
